# Test-only dependencies. Deliberately NOT in requirements.txt: that file is
# what Oryx pip-installs onto the App Service on every deploy, and a test
# runner has no business on a production instance — it is pure install time
# on a single core, repeated on every deploy.
#
# CI installs this file (it pulls in requirements.txt via -r); the deploy
# installs only requirements.txt.
-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
