{
  "mcpServers": {
    "azure-ai-search": {
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@ignitionai/azure-ai-search-mcp"],
      "env": {
        "AZURE_SEARCH_ENDPOINT": "${AZURE_SEARCH_ENDPOINT}",
        "AZURE_SEARCH_API_KEY": "${AZURE_SEARCH_API_KEY}",
        "AZURE_SEARCH_INDEX": "${AZURE_SEARCH_INDEX}"
      }
    }
  }
}
