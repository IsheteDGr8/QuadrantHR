# QuadrantHR portal (presentation layer from plan.md)

```bash
docker compose up -d          # postgres, redis, azurite, backend :8080
cd portal && npm install && npm run dev
```

Open http://localhost:5170 — sign in with `hr.admin@quadranthr.local` / `changeme123`.
