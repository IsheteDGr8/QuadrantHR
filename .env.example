# Shared local env for docker compose (copy to .env and fill as needed).
# Never commit real secrets.

# ClosedAI / Vera
SESSION_API_KEY=
LLM_PROVIDER=

# Optional Azure / OpenAI hooks (services that support mock mode ignore these)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=

# Inter-service DNS names on quadranthr-net (for future gateway / MCP wiring):
#   http://employee-directory:8000
#   http://resume-screening:8000
#   http://ticket-genie:8000
#   http://training-portal:8000
#   http://hr-copilot:8001
#   http://policy-generator:8000   (compose profile: full)
